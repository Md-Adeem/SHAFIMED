

// Query routes
   1.Remove Facilitator check code from query routes
   2. Add case submition validation ( No validation before saving the data from body)
   3. improve response type for all api of query

// Profile route
   1. add validation to the profile complete section there is no validation in the api

// Doctor route is of no use as of now we need to add dactor api in the route itself


// User Route : 
   1. Get doctor is declared three time in userroute , queryRoutes , doctorsRoute(depriciated);


// query model
   1. Mobile number is declared by string blunder